apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-broken
  namespace: demo-apps
  labels:
    app: nginx-broken
    scenario: readonly-filesystem
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx-broken
  template:
    metadata:
      labels:
        app: nginx-broken
    spec:
      containers:
        - name: nginx
          image: nginx:1.25
          ports:
            - containerPort: 80
          securityContext:
            # Removed readOnlyRootFilesystem to allow necessary writes
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 100m
              memory: 128Mi